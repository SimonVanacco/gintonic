{% extends 'base.html.twig' %}

{% block title %}Unit index{% endblock %}

{% block content %}
    <h1>Unit index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Singular</th>
                <th>Plural</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for unit in units %}
            <tr>
                <td>{{ unit.id }}</td>
                <td>{{ unit.singular }}</td>
                <td>{{ unit.plural }}</td>
                <td>
                    <a href="{{ path('unit_admin_show', {'id': unit.id}) }}">show</a>
                    <a href="{{ path('unit_admin_edit', {'id': unit.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('unit_admin_new') }}">Create new</a>
{% endblock %}
