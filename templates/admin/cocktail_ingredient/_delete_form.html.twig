<form method="post" action="{{ path('cocktail_ingredient_admin_delete', {'cocktail': cocktail.id, 'id': cocktail_ingredient.id}) }}" onsubmit="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cocktail_ingredient.id) }}">
    <button type="submit" class="btn btn-link"><i class="fas fa-trash"></i></button>
</form>
