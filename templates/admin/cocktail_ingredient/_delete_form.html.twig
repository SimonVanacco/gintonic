<a
        class=""
        href="{{ path('cocktail_ingredient_admin_delete', {'id': cocktail_ingredient.id, 'cocktail': cocktail.id, '_token': csrf_token('delete' ~ cocktail_ingredient.id)}) }}"
        onclick="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');"
>
    <i class="fas fa-trash"></i>
</a>
