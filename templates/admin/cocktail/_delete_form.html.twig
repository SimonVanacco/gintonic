<a
        class="btn btn-outline-danger"
        href="{{ path('cocktail_admin_delete', {'id': cocktail.id, '_token': csrf_token('delete' ~ cocktail.id)}) }}"
        onclick="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');"
>
    {{ 'Delete'|trans }}
</a>
