{% extends 'base_admin.html.twig' %}

{% block title %}Unit{% endblock %}

{% block content %}
    <h1>Unit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unit.id }}</td>
            </tr>
            <tr>
                <th>Singular</th>
                <td>{{ unit.singular }}</td>
            </tr>
            <tr>
                <th>Plural</th>
                <td>{{ unit.plural }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unit_admin_index') }}">back to list</a>

    <a href="{{ path('unit_admin_edit', {'id': unit.id}) }}">edit</a>

    {{ include('admin/unit/_delete_form.html.twig') }}
{% endblock %}
