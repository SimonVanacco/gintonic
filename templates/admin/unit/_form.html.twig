{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-primary">{{ 'Save'|trans }}</button>
    <a class="btn btn-outline-secondary" href="{{ path('unit_admin_index') }}">{{ 'Back to list'|trans }}</a>
    {% if unit is defined and unit.id %}
        {{ include('admin/unit/_delete_form.html.twig') }}
    {% endif %}
{{ form_end(form) }}
