<a
        class="btn btn-outline-danger"
        href="{{ path('unit_admin_delete', {'id': unit.id, '_token': csrf_token('delete' ~ unit.id)}) }}"
        onclick="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');"
>
    {{ 'Delete'|trans }}
</a>
