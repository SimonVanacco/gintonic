{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-primary">{{ 'Save'|trans }}</button>
    <a class="btn btn-outline-secondary" href="{{ path('ingredient_admin_index') }}">{{ 'Back to list'|trans }}</a>
    {% if ingredient is defined and ingredient.id %}
        {{ include('admin/ingredient/_delete_form.html.twig') }}
    {% endif %}
{{ form_end(form) }}
