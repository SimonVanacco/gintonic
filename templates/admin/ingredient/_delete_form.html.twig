<a
        class="btn btn-outline-danger"
        href="{{ path('ingredient_admin_delete', {'id': ingredient.id, '_token': csrf_token('delete' ~ ingredient.id)}) }}"
        onclick="return confirm('{{ 'Are you sure you want to delete this item?'|trans }}');"
>
    {{ 'Delete'|trans }}
</a>
