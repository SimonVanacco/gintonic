{% extends 'base_admin.html.twig' %}

{% block title %}Glass{% endblock %}

{% block content %}
    <h1>Glass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ glass.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ glass.name }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ glass.icon }}</td>
            </tr>
            <tr>
                <th>IconFamily</th>
                <td>{{ glass.iconFamily }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ glass.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('glass_admin_index') }}">{{ 'back to list'|trans }}</a>

    <a href="{{ path('glass_admin_edit', {'id': glass.id}) }}">edit</a>

    {{ include('admin/glass/_delete_form.html.twig') }}
{% endblock %}
